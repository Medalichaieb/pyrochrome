"""Pyrochrome — predict the post-firing render of a ceramic glaze from its chemistry.

Public, open-source, non-commercial (data is Glazy / CC BY-NC-SA 4.0).
See the package sub-modules:

- ``pyrochrome.pipeline`` — data download, cone mapping, atmosphere parsing, features.
- ``pyrochrome.models``   — baseline training, evaluation, export.
- ``pyrochrome.api``      — FastAPI prediction service.
"""

__version__ = "0.1.0"
